const initialState = {
    jwt: null,
    userId: null,
    username: null
}

export default (state = initialState, action = {}) => {
    switch (action.type) {
        default:
            return state;
    }
}